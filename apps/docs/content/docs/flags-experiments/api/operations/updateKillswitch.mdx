---
title: Update a killswitch
full: true
_openapi:
  method: PATCH
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Partial update applied to **every** env. Setting `value`/`switches`
          publishes a new version per env. Description-only patches don't bump
          versions.


          To change a single switch on a single env, use `PUT /{id}/switch`
          instead.


          **Use cases**


          - **Trip everywhere** — `{ "value": true }`. Kills the feature across
          dev/stage/prod in one call.

          - **Untrip everywhere** — `{ "value": false }`.

          - **Replace switches** — send the full new map; per-key edits use `PUT
          /{id}/switch`.

          - **Update description** — metadata-only patches don't bump versions.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Partial update applied to **every** env. Setting `value`/`switches` publishes a new version per env. Description-only patches don't bump versions.

To change a single switch on a single env, use `PUT /{id}/switch` instead.

**Use cases**

- **Trip everywhere** — `{ "value": true }`. Kills the feature across dev/stage/prod in one call.
- **Untrip everywhere** — `{ "value": false }`.
- **Replace switches** — send the full new map; per-key edits use `PUT /{id}/switch`.
- **Update description** — metadata-only patches don't bump versions.

<APIPage
  document={"../../packages/openapi/openapi.json"}
  operations={[{ path: "/api/admin/killswitches/{id}", method: "patch" }]}
/>
