---
title: Remove one switch entry
full: true
_openapi:
  method: DELETE
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Removes a single `switchKey` from the `switches` map on a single
          `env`. Publishes a new version on that env.


          Returns `{ removed: false }` if the entry didn't exist (idempotent
          no-op).


          **Use case:** Clean up a per-region override after the incident is
          resolved so the flat `value` governs again.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Removes a single `switchKey` from the `switches` map on a single `env`. Publishes a new version on that env.

Returns `{ removed: false }` if the entry didn't exist (idempotent no-op).

**Use case:** Clean up a per-region override after the incident is resolved so the flat `value` governs again.

<APIPage
  document={"../../packages/openapi/openapi.json"}
  operations={[{ path: "/api/admin/killswitches/{id}/switch", method: "delete" }]}
/>
