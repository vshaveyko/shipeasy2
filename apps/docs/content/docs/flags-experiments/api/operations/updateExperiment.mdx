---
title: Update an experiment
full: true
_openapi:
  method: PATCH
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Partial update. `allocation_pct`, `groups`, `salt`, `universe`,
          `params` are **immutable while running** — returns `409` if you try.
          Stop the experiment first.


          Editing `groups` while in `draft` is fine; weights must still sum to
          10000.


          **Use cases**


          - **Update metadata** — `description`, `tag`, `targeting_gate`
          editable any time.

          - **Ramp before launch** — set `allocation_pct` while still in
          `draft`.

          - **Tighten significance** — `significance_threshold` (Pro+).

          - **Rewire groups** — replace `groups` wholesale while in `draft`;
          immutable once running.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Partial update. `allocation_pct`, `groups`, `salt`, `universe`, `params` are **immutable while running** — returns `409` if you try. Stop the experiment first.

Editing `groups` while in `draft` is fine; weights must still sum to 10000.

**Use cases**

- **Update metadata** — `description`, `tag`, `targeting_gate` editable any time.
- **Ramp before launch** — set `allocation_pct` while still in `draft`.
- **Tighten significance** — `significance_threshold` (Pro+).
- **Rewire groups** — replace `groups` wholesale while in `draft`; immutable once running.

<APIPage
  document={"../../packages/openapi/openapi.json"}
  operations={[{ path: "/api/admin/experiments/{id}", method: "patch" }]}
/>
