{
  "name": "@shipeasy/react",
  "version": "1.0.0",
  "description": "ShipEasy React provider, hooks, and devtools integration",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "// build": "Build @shipeasy/sdk first so its /client subpath types resolve during our DTS step. Cloudflare Workers Builds runs `pnpm --filter @shipeasy/react build` directly (no turbo topology), so this prebuild guarantees a clean-checkout build succeeds.",
    "prebuild": "pnpm --filter @shipeasy/sdk build",
    "build": "tsup",
    "dev": "tsup --watch",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@shipeasy/sdk": "workspace:*"
  },
  "peerDependencies": {
    "react": ">=17.0.0",
    "react-dom": ">=17.0.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.7.4",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  },
  "sideEffects": false
}
